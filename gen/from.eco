/**
 * `str::from` for generate's error type, so `"{$e}"` works.
 *
 * A file of its own because a namespace is a property of the file.
 */

namespace str;

public function from(const vkgen::Error& $e) : string
{
    return $e->message();
}
